<!DOCTYPE html>
<html lang="en">

<head>
  <!-- Required meta tags -->
  <meta charset="utf-8">
  <style media="screen">
    /* :root {
      --main-bg-color: #AFD4CD;
      --main-color-2 : #89BEB4
    } */

    :root{
      --main-bg-color: #676767;
      --main-nav-color:#01ff09b3;
      --main-lightgreen-color:#28a745;
      --main-hover-blue:#99c6ff;
      /* --main-green-color: #20d225; */
       --main-green-color: #28a745;
    }
    /* * {
      border: 1px solid var(--main-lightgreen-color);
    } */
    .noHover {
      pointer-events: none;
    }

    body {
      background-color: var(--main-bg-color) !important;
    }
    .bugitem a {
      color: black;
    }

    #buglisthead ,#filternavbarhead {
      background-color: var(--main-green-color) !important;
      color: #fff!important;
      font-weight: 640;
    }

    #buglist {
      /* font-size: 1.3vw; */
    }

    #buglist a:hover {
      background-color:   var(--main-hover-blue)!important;
      color: black !important;
    }

    #dropdownsetting {
      background-color: #fae3d9;
      width: 400% ;
      /* font-size: 1.3vw !important; */
      padding :10px;
      text-align: center;

    }

    nav {
      background-color: var(--main-nav-color) !important;
      font-size: 1.4vw ;
      margin-bottom: 100px;
      height:80px;


    }
    nav a {
      color: black !important;
      font-size: 1.6vw ;
      font-weight: bold;
      border-radius: 10px !important;
      margin-top: 0px;
      margin-bottom: 0px;
      margin-left: 5px;
      margin-right: 5px;
    }
    nav a:hover {
      background-color:   var(--main-hover-blue) !important;
      color: #14171c !important;
    }
    .navbar-brand {
      font-size: 2.2vw !important;
    }

    .jumbotron {
      font-size: 70px !important;
      padding-top: 300px !important;
      padding: 2rem 1rem;
      margin-bottom: 2rem;
      background-color: var(--main-color-2) !important;
      border-radius: .3rem;
    }

    .centered {
      padding-top: 200px;
      text-align: center;
    }

    .sidenav1 {
      height: 100%; /* Full-height: remove this if you want "auto" height */
      width: 15%; /* Set the width of the sidebar */
      position: fixed; /* Fixed Sidebar (stay in place on scroll) */
      z-index: 1; /* Stay on top */
      top: 0; /* Stay at the top */
      left: 0;
      background-color: #; /* white */
      overflow-x: hidden; /* Disable horizontal scroll */
      /* padding-top: 20px; */
      margin-top:80px;
    }

    /* The navigation menu links */
    .sidenav1 a {
      background-color: #fff !important;
      border: 1px solid black;
      border-radius: 10px;
      padding: 6px 8px 6px 16px;
      text-decoration: none;
      font-size: 17px;
      color: #14171c !important;
      display: block;
    }

    /* When you mouse over the navigation links, change their color */
    .sidenav1 a:hover {
    background-color:   var(--main-hover-blue) !important;
      text-decoration: none;
      color: #14171c !important;
    }
    /* The sidebar menu */
    .sidenav {
      height: 100%; /* Full-height: remove this if you want "auto" height */
      width: 15%; /* Set the width of the sidebar */
      position: fixed; /* Fixed Sidebar (stay in place on scroll) */
      z-index: 1; /* Stay on top */
      top: 0; /* Stay at the top */
      left: 0;
      background-color: #; /* white */
      overflow-x: hidden; /* Disable horizontal scroll */
      /* padding-top: 20px; */
      margin-top:179px;
    }

    /* The navigation menu links */
    .sidenav a {
      background-color: #fff !important;
      border: 1px solid black;
      border-radius: 10px;
      padding: 6px 8px 6px 16px;
      text-decoration: none;
      font-size: 17px;
      color: #14171c !important;
      display: block;
    }

    /* When you mouse over the navigation links, change their color */
    .sidenav a:hover {
    background-color:   var(--main-hover-blue) !important;
      text-decoration: none;
      color: #14171c !important;
    }

    .rightsidenav {
      height: 100%; /* Full-height: remove this if you want "auto" height */
      width: 15%; /* Set the width of the sidebar */
      position: fixed; /* Fixed Sidebar (stay in place on scroll) */
      z-index: 1; /* Stay on top */
      top: 0; /* Stay at the top */
      left:1 ;
      background-color:  !important; /* white */
      overflow-x: hidden; /* Disable horizontal scroll */
      /* padding-top: 20px; */
      right: 0px !important;
      margin-top:81px;
    }

    /* The navigation menu links */
    .rightsidenav a {
      background-color: #fff !important;
      border: 1px solid black;
      border-radius: 10px;
      padding: 6px 8px 6px 16px;
      text-decoration: none;
      font-size: 17px;
      color: #14171c !important;
      display: block;
    }

    /* When you mouse over the navigation links, change their color */
    .rightsidenav a:hover {
    background-color:   var(--main-hover-blue) !important;
      text-decoration: none;
      color: #14171c !important;
    }

    /* Style page content */
    .main {
      margin-left: 160px; /* Same as the width of the sidebar */
      padding: 0px 10px;
    }

    /* On smaller screens, where height is less than 450px, change the style of the sidebar (less padding and a smaller font size) */
    @media screen and (max-height: 450px) {
      .sidenav {padding-top: 15px;}
      .sidenav a {font-size: 18px;}
    }

  </style>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <link rel="stylesheet" href="css/styles.css">
  <!-- Latest compiled and minified CSS -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script>
$(document).ready(function(){
  $("#rightsidemenubutton").click(function(){
    $("#rightsidenav").toggle();
  });
});
</script>

</head>

<body>
